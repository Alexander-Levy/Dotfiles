# Waybar
Status bar configuration with custom modules and styling.

## What's configured
- Module layout (left, center, right)
- CSS styling and colors
- Custom scripts for modules

## Dependencies
- `waybar`, `bc`, `cava`, `wpctl`, `wlctl`, `bluetui`
